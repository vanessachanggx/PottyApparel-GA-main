<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<style>
    body {
        font-family: 'Public Sans';
        background-color: #e6e2df;
        color: #343a40;
        margin: 0;
        padding: 0;
    }

    .container {
        margin-top: 2rem;
    }

    .card {
        padding: 40px;
        background-color: #f6f5f4;
        border-radius: 15px;
    }

    .cart-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        background-color: #f6f5f4;
        padding: 1rem;
        border-radius: 10px;
        margin-bottom: 1rem;
    }

    .cart-item img {
        width: 100px;
        object-fit: cover;
    }

    .cart-item .item-details {
        flex-grow: 1;
    }

    .cart-item .item-details h5 {
        margin: 0;
        font-size: 1rem;
        font-weight: bold;
    }

    .cart-item .item-details p {
        margin: 0.5rem 0;
    }

    .order-summary {
        text-align: right;
    }

    .order-summary .total {
        font-weight: bold;
        font-size: 1.2rem;
    }

    .btn-payment {
        background-color: #3f2c1b;
        color: white;
        border-radius: 5px;
        width: 100%;
    }

    .btn-payment:hover {
        background-color: #e6e2df;
        color: black;
    }
</style>
</head>
<body>
    <%- include('partial/navbar.ejs') %>

    <div class="container">
        <div class="card">
            <div class="row fs-1 text-center mb-4">
                <p>CHECKOUT</p>
            </div>

            <% if (cart.length === 0) { %>
                <!-- Empty cart message -->
                <div class="text-center">
                    <p>Your shopping cart is empty. <br>
                        <a href="/" class="btn btn-primary mt-3">Start Shopping Now!</a></p>
                </div>
            <% } else { %>
                <!-- Loop through the cart items -->
                <% for (let i = 0; i < cart.length; i++) { %>
                    <div class="cart-item">
                        <img src="/images/<%= cart[i].Image %>" alt="<%= cart[i].Name %>">

                        <div class="item-details">
                            <h5><a href="/product/<%= cart[i].ProductId %>" class="product-name"><%= cart[i].Name %></a></h5>
                            <p>Size: <%= cart[i].Size || 'N/A' %></p>
                            <p>Quantity: <%= cart[i].Quantity %></p>
                        </div>

                        <div class="item-price">
                            <p>Price: $<%= ((parseFloat(cart[i].Price) || 0) * cart[i].Quantity).toFixed(2) %></p>
                        </div>
                    </div>
                <% } %>

                <!-- Order Summary -->
                <div class="order-summary mt-4">
                    <h5>Order Summary</h5>
                    <hr>
                    <div class="d-flex justify-content-between">
                        <p>Cart Total</p>
                        <p>$<%= totalAmount.toFixed(2) %></p>
                    </div>
                    <div class="d-flex justify-content-between">
                        <p>Delivery Fee</p>
                        <p>$3.00</p>
                    </div>
                    <div class="d-flex justify-content-between total">
                        <p>Total</p>
                        <p>$<%= (totalAmount + 3).toFixed(2) %></p>
                    </div>
                </div>

                        <h6 class="mt-3">Payment Methods</h6>
                        <div class="row m-3 border-bottom fw-bold">
                            <div id="paypal-button-container"></div>
                            <% console.log(cart)%>
                            <input type="hidden" id="cartProducts" value='<%-JSON.stringify(cart)%>'>
                            <p id="result-message"></p>
                            <script src="https://sandbox.paypal.com/sdk/js?client-id=ATQQuSzP7fhPQBdI857o6No3EjYfFdblxq0No5LNnSonJRQwjPq_n3DUOK80Qdqve0PtrEvnGjuOcwcB&components=buttons&currency=SGD">
                            </script>
                            <script src="/js/paypal.js"> </script>
                         </div>

                        <button type="submit" class="btn btn-payment mt-4">Proceed to Payment</button>
                    </form>
                </div>
            <% } %>
        </div>
    </div>

    <%- include('partial/footer.ejs') %>
</body>
</html>
