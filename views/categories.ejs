<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href='https://fonts.googleapis.com/css?family=Parisienne' rel='stylesheet'>
    <link href='https://fonts.googleapis.com/css?family=Public Sans' rel='stylesheet'>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<body>

    <%- include ('partial/navbar.ejs') %>
    <div class="container">
      <div class="text-center"><h2>Categories</h2></div>
      <br>
      <table class="table table-hover small text-center">
        <thead>
            <tr>
              <th width="50">Image</th>
              <th width="100">Category Name</th>
              <th width="100">Description</th>             
              <th width="50">Edit</th>
              <th width="50">Delete</th>
            </tr>
        </thead>
        <tbody>
          <% for(let i=0; i < category.length; i++) { %>
            <tr>
              <td><img src = "images/<%= category[i].Image %>" width="80%"></td>
              <td><a href="/category/<%= category[i].CategoryID %>" ><%= category[i].Name %></a></td>             
              <td><%= category[i].Description %></td>
              <!-- Edit Link -->
              <td><a href="/editCategory/<%= category[i].CategoryID %>">Edit</a></td>
              <!-- Delete Link -->
              <td><a href="/deleteCategory/<%= category[i].CategoryID %>" onclick="return confirm('Are you sure you want to delete this category?')">Delete</a></td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
</body>
</html>