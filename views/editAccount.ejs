<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Account</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:wght@400;600&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<style>
    /* Full height container using Flexbox */
    body {
    font-family: 'Public Sans';
    background-color: #e6e2df; /* Matches the account page background */
    color: #343a40;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    height: 100vh; /* Full viewport height */
    
    }

    /* Header styling */
    header {
        background-color: #3f2c1b;
        color: white;
        padding: 10px 0;
    }

    /* Footer styling */
    footer {
        background-color: #3f2c1b;
        color: white;
        text-align: center;
        padding: 10px 0;
        margin-top: auto; /* Pushes footer to the bottom */
    }

    /* Centering the form container */
    .form-container {
        width: 50%; 
        padding: 20px;
        background-color: #f6f5f4;
        border-radius: 15px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
        margin-top: 40px;
        margin-bottom: 30px;
        margin-left: 400px;
        float: center;
        justify-content: center;
        text-align: center;
    }

    .form-title {
        font-size: 24px;
        font-weight: 600;
        margin-bottom: 20px;
        text-align: center;
    }

    .form-label {
        font-weight: bold;
    }

    .btn-save {
        background-color: #3f2c1b;
        color: white;
        border-radius: 5px;
        width: 100%;
        margin-bottom: 10px;
    }

    .btn-save:hover {
        background-color: #5e4b35;
    }

    .btn-cancel {
        background-color: rgba(0, 0, 0, 0.1);
        color: black;
        border-radius: 5px;
        width: 100%;
    }

    .btn-cancel:hover {
        background-color: rgba(0, 0, 0, 0.2);
    }

</style>
<body>
    <!-- Include Header -->
    <%- include('partial/navbar.ejs') %>

    <!-- Form Container -->
    <div class="form-container">
        <h1 class="form-title">Edit Account</h1>
        <form action="/account" method="POST" enctype="multipart/form-data">
            <!-- First Name -->
            <div class="mb-3">
                <label for="FirstName" class="form-label">First Name</label>
                <input type="text" class="form-control" id="FirstName" name="FirstName" placeholder="Enter your first name" value="<%= user.FirstName %>">
            </div>

            <!-- Last Name -->
            <div class="mb-3">
                <label for="LastName" class="form-label">Last Name</label>
                <input type="text" class="form-control" id="LastName" name="LastName" placeholder="Enter your last name" value="<%= user.LastName %>">
            </div>
            <!-- Profile Image -->
            <div class="mb-3">
                <label for="Image" class="form-label">Profile Image</label>
                <input type="file" class="form-control" id="Image" name="Image" value="<%= user.Image %>">
                <% if (user.Image) { %>
                    <img src="/images/<%= user.Image %>" alt="Profile Image" width="100">
                <% } %>
            </div>

            <!-- Buttons -->
            <button type="submit" class="btn btn-save">Save Changes</button>
            <a href="/account" class="btn btn-cancel">Cancel</a>
        </form>
    </div>

    <!-- Include Footer -->
    <%- include('partial/footer.ejs') %>
</body>
</html>
