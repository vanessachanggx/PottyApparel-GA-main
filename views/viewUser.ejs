<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details - POTTY APPARELs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <link href='https://fonts.googleapis.com/css?family=Public Sans' rel='stylesheet'>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            font-family: 'Public Sans';
            background-color: #e6e2df;
            color: #343a40;
        }

        .content-container {
            background-color: white;
            border-radius: 15px;
            padding: 2rem;
            margin-top: 2rem;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        .profile-image {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
        }

        .table {
            vertical-align: middle;
        }

        .action-btn {
            padding: 0.5rem 1rem;
            border-radius: 5px;
            text-decoration: none;
            transition: all 0.3s;
        }

        .edit-btn {
            background-color: #6C4F3D;
            color: white;
        }

        .edit-btn:hover {
            background-color: #5b3e2b;
            color: white;
        }

        .delete-btn {
            background-color: #dc3545;
            color: white;
        }

        .delete-btn:hover {
            background-color: #bb2d3b;
            color: white;
        }

        .role-badge {
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
        }

        .role-admin {
            background-color: #dc3545;
            color: white;
        }

        .role-user {
            background-color: #198754;
            color: white;
        }
    </style>
</head>
<body>
    <%- include ('partial/adminNavbar.ejs') %>
    
    <div class="container">
        <div class="content-container">
            <div class="text-center mb-4">
                <h2>User Profile</h2>
            </div>
            
            <div class="row">
                <div class="col-md-4 text-center">
                    <img src="/images/<%= user.Image %>" class="profile-image mb-3" alt="Profile Image">
                </div>
                <div class="col-md-8">
                    <table class="table">
                        <tbody>
                            <tr>
                                <th width="30%">Name</th>
                                <td><%= user.FirstName %> <%= user.LastName %></td>
                            </tr>
                            <tr>
                                <th>Email</th>
                                <td><%= user.Email %></td>
                            </tr>
                            <tr>
                                <th>Phone</th>
                                <td><%= user.PhoneNumber %></td>
                            </tr>
                            <tr>
                                <th>Role</th>
                                <td>
                                    <span class="role-badge <%= user.role === 'admin' ? 'role-admin' : 'role-user' %>">
                                        <%= user.role %>
                                    </span>
                                </td>
                            </tr>
                        </tbody>
                    </table>

                    <div class="d-flex justify-content-end gap-2 mt-4">
                        <a href="/editUser/<%= user.UserID %>" class="action-btn edit-btn">
                            <i class="fas fa-edit me-2"></i>Edit
                        </a>
                        <a href="/deleteUser/<%= user.UserID %>" 
                           class="action-btn delete-btn"
                           onclick="return confirm('Are you sure you want to delete this user?')">
                            <i class="fas fa-trash me-2"></i>Delete
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('partial/footer.ejs') %>
</body>
</html>
